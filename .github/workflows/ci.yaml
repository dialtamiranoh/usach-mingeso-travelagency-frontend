name: CI/CD Frontend

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create .env file
        run: |
          echo "VITE_TRAVELAGENCY_BACKEND_SERVER=${{ secrets.VITE_BACKEND_SERVER }}" >> .env
          echo "VITE_TRAVELAGENCY_BACKEND_PORT=${{ secrets.VITE_BACKEND_PORT }}" >> .env
          echo "VITE_KEYCLOAK_URL=${{ secrets.VITE_KEYCLOAK_URL }}" >> .env
          echo "VITE_KEYCLOAK_REALM=travelagency" >> .env
          echo "VITE_KEYCLOAK_CLIENT_ID=travelagency-frontend" >> .env

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/travelagency-frontend:latest

      - name: Deploy to AWS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script: |
            cd ~
            docker-compose pull frontend
            docker-compose up -d frontend